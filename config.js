export const config = {
    api_key: "CAP-", // get your key here https://www.capsolver.com/
    proxy: '', // tested with residential moblie proxy https://astroproxy.com, 'ip:port:login:password'
    retries: 20,
    check: true, // only check mode, put wallet addresses in file wallets.txt, eligible wallets will go into the file eligible.txt
    claim: false // claim mode, provide private keys in file wallets.txt
}
