export const config = {
    api_key: "CAP-", // get your key here https://capmonster.cloud/
    retries: 5,
    check: true, // only check mode, put wallet addresses in file wallets.txt, eligible wallets will go into the file eligible.txt
    claim: true // claim mode, provide private keys in file wallets.txt
}